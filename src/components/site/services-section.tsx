import type { PublicService } from "@/lib/crm/public-data";
import { getServiceIcon } from "./service-icons";

export function ServicesSection({ services }: { services: PublicService[] }) { return <section id="services" className="bg-surface-low/30 py-20 sm:py-24 lg:py-32"><div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-10 xl:px-16"><div className="mb-10 sm:mb-16"><h2 className="text-3xl font-bold text-primary md:text-4xl">Our Expertise</h2><div className="mt-4 h-1 w-24 rounded-full bg-secondary" /></div><div className="grid gap-4 sm:grid-cols-2 sm:gap-6 lg:grid-cols-4">{services.map(({ slug, title, description, icon_name }) => { const Icon = getServiceIcon(icon_name); return <article id={slug} key={slug} className={`scroll-mt-24 glass-card rounded-2xl p-6 sm:p-8 ${slug === "web-custom-systems" ? "lg:col-span-2" : ""}`}><Icon className="mb-4 text-secondary" size={40} /><h3 className="text-xl font-bold text-foreground">{title}</h3><p className="mt-3 text-sm leading-6 text-muted">{description}</p></article>; })}</div></div></section>; }
